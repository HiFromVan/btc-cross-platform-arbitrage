# 项目协作约定

- 始终使用简体中文沟通。
- 第一阶段仅允许公开行情、Mock 数据、机会计算和模拟执行；禁止真实资金与真实下单。
- 金额、价格、数量、费用和核心计算使用 `rust_decimal::Decimal`，不得使用 `f64`。
- 时间统一使用 UTC；外部时间戳必须明确单位与精度。
- 先完成市场匹配与结算兼容性验证，再进入套利计算。
- 每个阶段完成后运行 `cargo check --workspace` 与 `cargo test --workspace`。
- 凭据只能通过环境变量注入，禁止写入代码或提交版本库。

